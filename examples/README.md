# examples

`infer.py` — v3 모델 hosted inference. `ROBOFLOW_API_KEY` 필요.

```bash
export ROBOFLOW_API_KEY=...
python examples/infer.py https://example.com/pack.jpg
```

권고 confidence는 0.42. 워크스페이스 크레딧이 없으면 402가 난다.
